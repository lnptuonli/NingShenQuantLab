# 🚀 量化调度平台架构设计与实施指南

## 📋 项目定位

**当前项目：** NingShenQuantLab - 量化研究后端调度平台  
**核心职责：** 将 Python 量化工具（数据获取、特征计算、模型训练等）封装为 RESTful API，提供统一的调度和管理能力  
**技术架构：** Spring Boot 后端 + Python 脚本引擎 + 未来 Vue 前端  

---

## 🎯 系统架构设计

### 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        Vue 前端（未来）                          │
│              可视化界面、任务监控、日志查看                       │
└────────────────────────────┬────────────────────────────────────┘
                             │ HTTP/WebSocket
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│                   Spring Boot 后端（当前）                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Controller 层：接收 API 请求、参数校验、返回响应        │   │
│  └──────────────────────┬──────────────────────────────────┘   │
│                         ↓                                        │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Service 层：业务逻辑、任务调度、状态管理                │   │
│  └──────────────┬───────────────────┬──────────────────────┘   │
│                 ↓                   ↓                            │
│  ┌──────────────────────┐  ┌──────────────────────────┐        │
│  │ Python Executor 层   │  │  DAO 层：任务持久化       │        │
│  │ 脚本执行、进程管理   │  │  日志存储、状态查询       │        │
│  └──────────┬───────────┘  └───────────┬──────────────┘        │
│             ↓                           ↓                        │
└─────────────┼───────────────────────────┼────────────────────────┘
              ↓                           ↓
   ┌──────────────────────┐    ┌──────────────────────┐
   │   Python 脚本         │    │     MySQL 数据库      │
   │  feature_manager.py   │    │   任务表、日志表      │
   │  data_fetcher.py      │    │   执行记录表          │
   │  model_trainer.py     │    └──────────────────────┘
   └──────────────────────┘
```

---

## 📂 新增模块设计

### 1. 核心模块规划

基于您的需求，建议新增以下模块：

```
src/main/java/com/ningshenquantlab/alphaforge_demo1/
├── 【新增】quant/                              # 🔬 量化业务核心模块
│   ├── controller/
│   │   ├── FeatureController.java              # 特征计算 API
│   │   ├── DataController.java                 # 数据获取 API
│   │   ├── ModelController.java                # 模型训练 API
│   │   └── TaskController.java                 # 任务管理 API
│   │
│   ├── service/
│   │   ├── FeatureService.java                 # 特征计算服务
│   │   ├── DataService.java                    # 数据服务
│   │   ├── ModelService.java                   # 模型服务
│   │   └── TaskService.java                    # 任务管理服务
│   │
│   ├── entity/
│   │   ├── TaskRecord.java                     # 任务记录实体
│   │   ├── TaskLog.java                        # 任务日志实体
│   │   └── TaskStatus.java                     # 任务状态枚举
│   │
│   ├── dto/
│   │   ├── FeatureCalcRequest.java             # 特征计算请求 DTO
│   │   ├── TaskQueryRequest.java               # 任务查询请求 DTO
│   │   └── TaskResponse.java                   # 任务响应 DTO
│   │
│   └── dao/
│       ├── TaskRecordDao.java                  # 任务记录 DAO
│       └── TaskLogDao.java                     # 任务日志 DAO
│
├── 【新增】executor/                           # ⚙️ Python 执行引擎
│   ├── PythonExecutor.java                     # Python 执行器接口
│   ├── LocalPythonExecutor.java                # 本地 Python 执行器实现
│   ├── ProcessManager.java                     # 进程管理器
│   ├── CommandBuilder.java                     # 命令构建器
│   └── OutputCollector.java                    # 输出收集器
│
├── 【新增】async/                              # 🔄 异步任务管理
│   ├── AsyncConfig.java                        # 异步配置
│   ├── AsyncTaskExecutor.java                  # 异步任务执行器
│   └── TaskQueue.java                          # 任务队列
│
├── 【新增】monitor/                            # 📊 监控模块
│   ├── TaskMonitor.java                        # 任务监控
│   ├── ResourceMonitor.java                    # 资源监控（CPU、内存）
│   └── AlertService.java                       # 告警服务（可选）
│
├── 【新增】config/                             # ⚙️ 配置模块
│   ├── PythonConfig.java                       # Python 环境配置
│   ├── ScriptConfig.java                       # 脚本路径配置
│   └── ExecutorConfig.java                     # 执行器配置
│
└── 【现有】common/                             # 📦 公共模块（复用）
    ├── PageResult.java
    ├── Result.java
    └── 【新增】enums/
        ├── TaskStatusEnum.java                 # 任务状态枚举
        └── TaskTypeEnum.java                   # 任务类型枚举
```

---

## 🗄️ 数据库设计

### 1. 任务记录表（task_record）

```sql
CREATE TABLE `task_record` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '任务ID',
  `task_name` VARCHAR(100) NOT NULL COMMENT '任务名称',
  `task_type` VARCHAR(50) NOT NULL COMMENT '任务类型（FEATURE_CALC/DATA_FETCH/MODEL_TRAIN）',
  `status` VARCHAR(20) NOT NULL DEFAULT 'PENDING' COMMENT '任务状态（PENDING/RUNNING/SUCCESS/FAILED/CANCELLED）',
  `command` TEXT NOT NULL COMMENT '执行的完整命令',
  `parameters` JSON COMMENT '任务参数（JSON格式）',
  `start_time` DATETIME COMMENT '开始时间',
  `end_time` DATETIME COMMENT '结束时间',
  `duration` INT COMMENT '执行时长（秒）',
  `exit_code` INT COMMENT '退出码',
  `error_message` TEXT COMMENT '错误信息',
  `created_by` VARCHAR(50) COMMENT '创建人',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_status` (`status`),
  KEY `idx_task_type` (`task_type`),
  KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='任务记录表';
```

### 2. 任务日志表（task_log）

```sql
CREATE TABLE `task_log` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '日志ID',
  `task_id` BIGINT(20) NOT NULL COMMENT '任务ID',
  `log_type` VARCHAR(20) NOT NULL COMMENT '日志类型（STDOUT/STDERR/INFO/ERROR）',
  `log_content` TEXT NOT NULL COMMENT '日志内容',
  `log_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '日志时间',
  PRIMARY KEY (`id`),
  KEY `idx_task_id` (`task_id`),
  KEY `idx_log_time` (`log_time`),
  FOREIGN KEY (`task_id`) REFERENCES `task_record`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='任务日志表';
```

### 3. 特征计算记录表（feature_calc_record）（可选）

```sql
CREATE TABLE `feature_calc_record` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '记录ID',
  `task_id` BIGINT(20) NOT NULL COMMENT '关联任务ID',
  `start_date` DATE NOT NULL COMMENT '开始日期',
  `end_date` DATE NOT NULL COMMENT '结束日期',
  `force_recalc` TINYINT(1) DEFAULT 0 COMMENT '是否强制重算',
  `feature_count` INT COMMENT '计算的特征数量',
  `success_count` INT COMMENT '成功数量',
  `failed_count` INT COMMENT '失败数量',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_task_id` (`task_id`),
  KEY `idx_date_range` (`start_date`, `end_date`),
  FOREIGN KEY (`task_id`) REFERENCES `task_record`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='特征计算记录表';
```

---

## 🔧 配置文件扩展

### application.yml 新增配置

```yaml
# Python 执行配置
python:
  # Python 解释器路径（支持虚拟环境）
  interpreter: "D:/anaconda3/envs/quant/python.exe"  # Windows 示例
  # interpreter: "/home/user/anaconda3/envs/quant/bin/python"  # Linux 示例
  
  # 脚本根目录
  script-path: "D:/QuantScripts"
  
  # 脚本配置
  scripts:
    feature-manager: "feature_manager.py"
    data-fetcher: "data_fetcher.py"
    model-trainer: "model_trainer.py"
  
  # 执行配置
  execution:
    timeout: 3600  # 超时时间（秒），默认1小时
    max-concurrent-tasks: 5  # 最大并发任务数
    enable-log-capture: true  # 是否捕获日志
    log-buffer-size: 8192  # 日志缓冲区大小

# 异步任务配置
async:
  executor:
    core-pool-size: 5
    max-pool-size: 10
    queue-capacity: 100
    thread-name-prefix: "quant-async-"
    
# 任务监控配置
task:
  monitor:
    enabled: true
    check-interval: 5000  # 检查间隔（毫秒）
    auto-retry: true  # 失败自动重试
    max-retry-times: 3  # 最大重试次数
```

---

## 💡 核心代码设计示例

### 1. Python 执行器接口设计

```java
/**
 * Python 脚本执行器接口
 */
public interface PythonExecutor {
    
    /**
     * 同步执行 Python 脚本
     * @param scriptName 脚本名称
     * @param args 参数列表
     * @return 执行结果
     */
    ExecutionResult executeSync(String scriptName, List<String> args);
    
    /**
     * 异步执行 Python 脚本
     * @param scriptName 脚本名称
     * @param args 参数列表
     * @return 任务ID
     */
    CompletableFuture<ExecutionResult> executeAsync(String scriptName, List<String> args);
    
    /**
     * 取消正在执行的任务
     * @param taskId 任务ID
     */
    void cancelTask(Long taskId);
    
    /**
     * 获取任务状态
     * @param taskId 任务ID
     * @return 任务状态
     */
    TaskStatus getTaskStatus(Long taskId);
}
```

### 2. 特征计算 API 设计

```java
/**
 * 特征计算 Controller
 */
@RestController
@RequestMapping("/api/v1/features")
public class FeatureController {
    
    private final FeatureService featureService;
    
    /**
     * 计算特征
     * POST /api/v1/features/calculate
     * Body: {
     *   "startDate": "20250301",
     *   "endDate": "20250305",
     *   "force": false,
     *   "async": true
     * }
     */
    @PostMapping("/calculate")
    public Result<TaskResponse> calculateFeatures(
            @RequestBody @Validated FeatureCalcRequest request) {
        TaskResponse task = featureService.calculateFeatures(request);
        return Result.success(task);
    }
    
    /**
     * 查询任务状态
     * GET /api/v1/features/tasks/{taskId}
     */
    @GetMapping("/tasks/{taskId}")
    public Result<TaskResponse> getTaskStatus(@PathVariable Long taskId) {
        TaskResponse task = featureService.getTaskStatus(taskId);
        return Result.success(task);
    }
    
    /**
     * 获取任务日志
     * GET /api/v1/features/tasks/{taskId}/logs
     */
    @GetMapping("/tasks/{taskId}/logs")
    public Result<List<TaskLog>> getTaskLogs(@PathVariable Long taskId) {
        List<TaskLog> logs = featureService.getTaskLogs(taskId);
        return Result.success(logs);
    }
    
    /**
     * 取消任务
     * POST /api/v1/features/tasks/{taskId}/cancel
     */
    @PostMapping("/tasks/{taskId}/cancel")
    public Result<Void> cancelTask(@PathVariable Long taskId) {
        featureService.cancelTask(taskId);
        return Result.success();
    }
}
```

### 3. 命令构建器设计

```java
/**
 * Python 命令构建器
 */
@Component
public class CommandBuilder {
    
    @Value("${python.interpreter}")
    private String pythonPath;
    
    @Value("${python.script-path}")
    private String scriptPath;
    
    /**
     * 构建特征计算命令
     * 示例：python feature_manager.py calc --start 20250301 --end 20250305 --force
     */
    public List<String> buildFeatureCalcCommand(
            String startDate, String endDate, boolean force) {
        
        List<String> command = new ArrayList<>();
        command.add(pythonPath);
        command.add(Paths.get(scriptPath, "feature_manager.py").toString());
        command.add("calc");
        command.add("--start");
        command.add(startDate);
        command.add("--end");
        command.add(endDate);
        
        if (force) {
            command.add("--force");
        }
        
        return command;
    }
}
```

---

## 📝 API 接口设计

### 1. 特征计算模块

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/features/calculate` | 提交特征计算任务 |
| GET | `/api/v1/features/tasks` | 查询任务列表（分页） |
| GET | `/api/v1/features/tasks/{taskId}` | 查询任务详情 |
| GET | `/api/v1/features/tasks/{taskId}/logs` | 获取任务日志 |
| POST | `/api/v1/features/tasks/{taskId}/cancel` | 取消任务 |
| POST | `/api/v1/features/tasks/{taskId}/retry` | 重试失败任务 |

### 2. 数据获取模块

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/data/fetch` | 提交数据获取任务 |
| GET | `/api/v1/data/tasks` | 查询数据任务列表 |
| GET | `/api/v1/data/tasks/{taskId}` | 查询数据任务详情 |

### 3. 模型训练模块

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/models/train` | 提交模型训练任务 |
| GET | `/api/v1/models/tasks` | 查询训练任务列表 |
| GET | `/api/v1/models/tasks/{taskId}` | 查询训练任务详情 |
| GET | `/api/v1/models/tasks/{taskId}/metrics` | 获取训练指标 |

### 4. 任务管理模块（通用）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/tasks` | 查询所有任务（分页、过滤） |
| GET | `/api/v1/tasks/{taskId}` | 查询任务详情 |
| DELETE | `/api/v1/tasks/{taskId}` | 删除任务记录 |
| GET | `/api/v1/tasks/statistics` | 获取任务统计信息 |

---

## 🎯 实施步骤（详细待办清单）

### 阶段一：基础设施搭建（1-2天）

#### ✅ 步骤 1：创建目录结构
```bash
# 在 src/main/java/com/ningshenquantlab/alphaforge_demo1/ 下创建
mkdir quant quant/controller quant/service quant/entity quant/dto quant/dao
mkdir executor async monitor config
mkdir common/enums
```

#### ✅ 步骤 2：数据库表创建
- [ ] 创建 `task_record` 表
- [ ] 创建 `task_log` 表
- [ ] 创建 `feature_calc_record` 表（可选）
- [ ] 准备测试数据

#### ✅ 步骤 3：配置文件扩展
- [ ] 在 `application.yml` 中添加 Python 配置
- [ ] 添加异步任务配置
- [ ] 添加任务监控配置
- [ ] 配置日志输出路径

#### ✅ 步骤 4：实体类创建
- [ ] `TaskRecord.java` - 任务记录实体
- [ ] `TaskLog.java` - 任务日志实体
- [ ] `TaskStatusEnum.java` - 任务状态枚举
- [ ] `TaskTypeEnum.java` - 任务类型枚举
- [ ] `ExecutionResult.java` - 执行结果封装

---

### 阶段二：Python 执行引擎（2-3天）

#### ✅ 步骤 5：核心执行器开发
- [ ] `PythonExecutor.java` - 接口定义
- [ ] `LocalPythonExecutor.java` - 本地执行器实现
- [ ] `ProcessManager.java` - 进程管理器
- [ ] `CommandBuilder.java` - 命令构建器
- [ ] `OutputCollector.java` - 输出收集器

#### ✅ 步骤 6：异步任务支持
- [ ] `AsyncConfig.java` - 异步配置
- [ ] `AsyncTaskExecutor.java` - 异步执行器
- [ ] `TaskQueue.java` - 任务队列管理

#### ✅ 步骤 7：单元测试
- [ ] 测试简单 Python 脚本执行
- [ ] 测试参数传递
- [ ] 测试日志捕获
- [ ] 测试异常处理
- [ ] 测试进程超时
- [ ] 测试任务取消

---

### 阶段三：特征计算模块（3-4天）

#### ✅ 步骤 8：DTO 设计
- [ ] `FeatureCalcRequest.java` - 特征计算请求
- [ ] `TaskQueryRequest.java` - 任务查询请求
- [ ] `TaskResponse.java` - 任务响应

#### ✅ 步骤 9：DAO 层开发
- [ ] `TaskRecordDao.java` + 实现类
- [ ] `TaskLogDao.java` + 实现类
- [ ] MyBatis Mapper XML 配置

#### ✅ 步骤 10：Service 层开发
- [ ] `FeatureService.java` - 接口定义
- [ ] `FeatureServiceImpl.java` - 实现类
  - 任务创建逻辑
  - 调用 Python 执行器
  - 日志记录
  - 状态更新

#### ✅ 步骤 11：Controller 层开发
- [ ] `FeatureController.java`
  - 计算特征接口
  - 查询任务状态接口
  - 获取任务日志接口
  - 取消任务接口

#### ✅ 步骤 12：集成测试
- [ ] 测试完整的特征计算流程
- [ ] 测试强制重算参数
- [ ] 测试异步执行
- [ ] 测试任务取消
- [ ] 测试错误处理

---

### 阶段四：监控和管理（1-2天）

#### ✅ 步骤 13：任务监控
- [ ] `TaskMonitor.java` - 任务状态监控
- [ ] `ResourceMonitor.java` - 资源监控（CPU、内存）
- [ ] 定时检查任务状态
- [ ] 超时任务处理

#### ✅ 步骤 14：任务管理 API
- [ ] `TaskController.java` - 通用任务管理
- [ ] 任务列表查询（分页、过滤）
- [ ] 任务统计接口
- [ ] 批量操作接口

---

### 阶段五：扩展其他模块（按需）

#### ✅ 步骤 15：数据获取模块
- [ ] `DataController.java`
- [ ] `DataService.java`
- [ ] 参照特征计算模块实现

#### ✅ 步骤 16：模型训练模块
- [ ] `ModelController.java`
- [ ] `ModelService.java`
- [ ] 训练指标记录

---

### 阶段六：优化和完善（持续）

#### ✅ 步骤 17：性能优化
- [ ] 连接池优化
- [ ] 异步任务队列优化
- [ ] 日志存储优化（大日志文件处理）

#### ✅ 步骤 18：文档完善
- [ ] API 文档（Swagger）
- [ ] 部署文档
- [ ] 运维文档

#### ✅ 步骤 19：监控告警
- [ ] 任务失败告警
- [ ] 资源占用告警
- [ ] 邮件/钉钉通知

---

## 🔑 关键技术点

### 1. Python 进程调用（Windows）

```java
ProcessBuilder pb = new ProcessBuilder(
    "D:/anaconda3/envs/quant/python.exe",
    "D:/QuantScripts/feature_manager.py",
    "calc",
    "--start", "20250301",
    "--end", "20250305"
);

// 设置工作目录
pb.directory(new File("D:/QuantScripts"));

// 合并错误流
pb.redirectErrorStream(true);

// 启动进程
Process process = pb.start();

// 捕获输出
BufferedReader reader = new BufferedReader(
    new InputStreamReader(process.getInputStream(), StandardCharsets.UTF_8));

String line;
while ((line = reader.readLine()) != null) {
    // 保存到数据库或日志文件
    saveLog(taskId, line);
}

// 等待完成
int exitCode = process.waitFor();
```

### 2. 异步任务管理

```java
@Configuration
@EnableAsync
public class AsyncConfig {
    
    @Bean(name = "quantTaskExecutor")
    public Executor taskExecutor() {
        ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
        executor.setCorePoolSize(5);
        executor.setMaxPoolSize(10);
        executor.setQueueCapacity(100);
        executor.setThreadNamePrefix("quant-");
        executor.initialize();
        return executor;
    }
}

// 使用
@Async("quantTaskExecutor")
public CompletableFuture<ExecutionResult> executeAsync(String script, List<String> args) {
    ExecutionResult result = executePython(script, args);
    return CompletableFuture.completedFuture(result);
}
```

### 3. 任务状态管理

```java
public enum TaskStatusEnum {
    PENDING("待执行"),
    RUNNING("执行中"),
    SUCCESS("成功"),
    FAILED("失败"),
    CANCELLED("已取消"),
    TIMEOUT("超时");
    
    private final String description;
}
```

### 4. 日志实时收集

建议使用 **数据库 + 文件** 双存储：
- 数据库：存储关键日志、错误信息（便于查询）
- 文件：存储完整日志（避免数据库过大）

---

## 🚨 注意事项和最佳实践

### 1. 安全性
- ⚠️ **命令注入防护**：严格校验所有参数，避免 SQL 注入式的命令注入
- ⚠️ **权限控制**：限制可执行的脚本范围
- ⚠️ **参数白名单**：只允许预定义的参数和值

### 2. 稳定性
- ✅ **超时处理**：设置合理的超时时间，避免僵尸进程
- ✅ **异常重试**：失败任务自动重试机制
- ✅ **资源限制**：限制最大并发任务数
- ✅ **优雅关闭**：应用关闭时终止所有运行中的任务

### 3. 可维护性
- ✅ **日志规范**：统一日志格式，便于排查问题
- ✅ **监控指标**：任务成功率、平均耗时、资源占用
- ✅ **配置外部化**：所有路径、参数都通过配置文件管理

### 4. 扩展性
- ✅ **接口抽象**：执行器接口化，未来可支持远程执行（SSH、K8s）
- ✅ **插件化**：不同类型任务（特征、训练、回测）可以独立扩展
- ✅ **消息队列**：未来可接入 RabbitMQ/Kafka 实现分布式调度

---

## 📊 推荐技术选型补充

根据项目规模，建议考虑以下技术：

### 必选（当前阶段）
| 技术 | 用途 | 优先级 |
|------|------|--------|
| Spring Boot | 后端框架 | ⭐⭐⭐⭐⭐ |
| MyBatis | 数据持久化 | ⭐⭐⭐⭐⭐ |
| Spring Async | 异步任务 | ⭐⭐⭐⭐⭐ |
| ProcessBuilder | Python 调用 | ⭐⭐⭐⭐⭐ |

### 可选（未来扩展）
| 技术 | 用途 | 优先级 |
|------|------|--------|
| RabbitMQ/Kafka | 消息队列（分布式调度） | ⭐⭐⭐ |
| Redis | 任务队列、缓存 | ⭐⭐⭐⭐ |
| WebSocket | 实时日志推送 | ⭐⭐⭐ |
| Quartz | 定时任务（定时计算特征） | ⭐⭐⭐⭐ |
| Prometheus + Grafana | 监控告警 | ⭐⭐⭐ |
| Docker | 容器化部署 | ⭐⭐⭐ |
| Kubernetes | 容器编排（大规模） | ⭐⭐ |

---

## 🎓 学习资源推荐

1. **Spring Boot 异步任务**：`@Async` 注解、`CompletableFuture`
2. **Java 进程管理**：`ProcessBuilder`、`Process` API
3. **任务调度框架**：Spring `@Scheduled`、Quartz
4. **WebSocket**：实时日志推送（未来前端需要）

---

## 📅 开发时间估算

| 阶段 | 预计时间 | 说明 |
|------|----------|------|
| 阶段一：基础设施 | 1-2天 | 目录、数据库、配置 |
| 阶段二：执行引擎 | 2-3天 | Python 调用核心 |
| 阶段三：特征计算 | 3-4天 | 完整业务流程 |
| 阶段四：监控管理 | 1-2天 | 任务监控、管理 API |
| 阶段五：扩展模块 | 按需 | 数据获取、模型训练 |
| **总计** | **7-11天** | 第一个可用版本 |

---

## 🔄 迭代计划

### V1.0（当前目标）
- ✅ 特征计算 API
- ✅ 任务管理（CRUD）
- ✅ 日志查看
- ✅ 基础监控

### V1.1（优化）
- 定时任务支持
- 任务失败重试
- WebSocket 实时日志

### V2.0（扩展）
- 数据获取模块
- 模型训练模块
- 性能优化

### V3.0（分布式）
- Redis 任务队列
- 消息队列集成
- 多机部署支持

---

## 📞 常见问题 FAQ

### Q1：Python 脚本执行失败怎么办？
**A：** 
1. 检查 Python 路径配置是否正确
2. 检查脚本路径是否正确
3. 查看任务日志中的错误信息
4. 手动在命令行测试脚本是否可执行

### Q2：如何处理长时间运行的任务？
**A：**
1. 使用异步执行（`@Async`）
2. 设置合理的超时时间
3. 提供任务取消接口
4. 前端轮询或 WebSocket 获取进度

### Q3：日志太大怎么办？
**A：**
1. 日志文件单独存储，数据库只存关键信息
2. 定期清理旧日志
3. 使用日志滚动（Logback 配置）

### Q4：如何支持定时任务？
**A：**
在 Service 层添加 `@Scheduled` 注解：
```java
@Scheduled(cron = "0 0 2 * * ?")  // 每天凌晨2点执行
public void scheduledFeatureCalc() {
    // 自动计算昨天的特征
}
```

---

## 📄 总结

这份架构设计为您提供了：
1. ✅ **清晰的模块划分**：Controller → Service → Executor → Python
2. ✅ **完整的数据库设计**：任务记录、日志存储
3. ✅ **详细的实施步骤**：分 6 个阶段，每个步骤都有明确待办
4. ✅ **核心技术示例**：Python 调用、异步任务、状态管理
5. ✅ **最佳实践建议**：安全性、稳定性、可扩展性

**建议从阶段一开始，按步骤实施，先完成特征计算模块作为原型，再逐步扩展其他功能。**

---

*文档生成时间：2025-12-29*  
*作者：AI 架构师*  
*版本：V1.0*

